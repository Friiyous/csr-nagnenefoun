# EPS Manager - Configuration Supabase (Production)

# URL Supabase (Project Settings → API)
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Clé anon publique (Project Settings → API → anon public key)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Service role key (UNIQUEMENT pour les operations serveur)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Application
NEXT_PUBLIC_APP_NAME=EPS Manager